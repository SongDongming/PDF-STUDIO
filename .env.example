# Public browser configuration only. Never place API keys in a Vite environment.
VITE_API_BASE_URL=http://localhost:18800/api/v1

# 本地/局域网部署时的固定端口（栈脚本使用；WSL 模式可用 --lan-host 显式指定）。
PDFWIKI_FRONTEND_PORT=4321
PDFWIKI_API_PORT=18800
PDFWIKI_OCR_TUNNEL_PORT=18111
